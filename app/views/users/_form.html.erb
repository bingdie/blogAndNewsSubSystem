<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name,"用户名：" %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :password, '密码：' %><br />
    <%= f.text_field :password %>
  </div>
  <div class="field">
    <%= f.label :password_confirmation, '再输入一次：' %><br />
    <%= f.text_field :password_confirmation %>
  </div>
    <div class="field">
      <%= f.label :user_type, '用户权限：' %><br />
      <%= f.text_field :user_type %>
    </div>
  <div class="actions">
    <%= f.submit  %>
  </div>
<% end %>
